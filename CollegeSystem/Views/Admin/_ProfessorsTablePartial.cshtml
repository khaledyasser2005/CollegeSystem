@model List<CollegeSystem.Models.Professor>

@if (Model != null && Model.Any())
{
    <table class="w-full" style="border-collapse:collapse;">
        <thead>
            <tr style="background:#3b4f9e;">
                <th style="text-align:left;padding:14px;color:white;">ID</th>
                <th style="text-align:left;padding:14px;color:white;">Name</th>
                <th style="text-align:left;padding:14px;color:white;">Email</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var prof in Model)
            {
                <tr style="border-bottom:1px solid #f0f0f0;">
                    <td style="padding:10px;">@prof.ID</td>
                    <td style="padding:10px;">@prof.Name</td>
                    <td style="padding:10px;">@prof.Email</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-info">No Professors Found</div>
}